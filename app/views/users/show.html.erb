<h1>Users#show</h1>
<p> Page of <%= @user.username %> </p>
<p>its playlists</p>
<% @playlists.each do |playlist| %>
  <div><%= link_to playlist.name, playlist_path(playlist) %></div>
<% end %>
<p>playlists followed</p>
<% @user.followed_playlists.each do |x| %>
  <div><%= link_to x.playlist.name, playlist_path(x.playlist) %></div>
<% end %>
<p>people he follows</p>
